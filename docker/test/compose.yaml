services:
  backend:
    build:
      context: ../..
      dockerfile: docker/test/Dockerfile
    env_file:
      - ../../backend/.env.example
    ports:
      - "2137:2137"
    depends_on:
      db:
        condition: service_healthy
  db:
    image: postgres
    restart: always
    user: postgres
    volumes:
      - db-data:/var/lib/postgresql
    env_file:
      - ../../backend/.env.example
    expose:
      - 5432
    healthcheck:
      test: [ "CMD", "pg_isready" ]
      interval: 10s
      timeout: 5s
      retries: 5
volumes:
  db-data:
